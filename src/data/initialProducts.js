// Products start EMPTY — a vendor must log in and add items from their dashboard.
// This matches the real product lifecycle: products only exist after a seller creates them.
export const initialProducts = [];
